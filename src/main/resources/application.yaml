spring:
  application:
    name: focusider
  datasource:
    url: jdbc:mariadb://localhost:3307/sourcedb
    username: producer
    password: producer!!3732
  jpa:
    hibernate:
      ddl-auto: update
  data:
    redis:
      host: localhost
      port: 6379
jwt:
  access:
    expiration: 300000 # 5 minutes
  refresh:
    expiration: 86400000 # 24 hours

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web.servlet.mvc.method.annotation: TRACE
